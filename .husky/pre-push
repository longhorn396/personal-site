# Run tests and enforce minimum coverage thresholds (90% global for statements, branches, functions, and lines).
# If coverage is below threshold, Jest exits non-zero and Husky prevents the push.
npm run test -- --coverage --coverageThreshold='{"global":{"statements":90,"branches":90,"functions":90,"lines":90}}'
